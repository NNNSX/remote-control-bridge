# UI QA Checklist

每次修改 `assets/index.html`、`assets/app.css` 或 `assets/app.js` 后，至少检查以下状态：

## 页面状态

- 连接前：只显示连接表单和待机状态，不展示一堆不可用操作。
- 连接成功：仪表盘、GPU、终端、日志、文件管理全部可用。
- 连接断开或会话过期：操作控件恢复禁用，待机状态重新出现。
- Agent 关闭和开启：权限开关状态与后端响应一致。

## 视口尺寸

- 桌面：1440 x 1000。
- 对齐桌面：1280 x 720，主要标签底部应与视口底部对齐。
- 低高度桌面：1280 x 600，文件和任务使用固定保底高度，内容在内部滚动。
- 窄桌面：1024 x 768。
- 移动端：390 x 844。

## 几何检查

- `document.documentElement.scrollWidth === document.documentElement.clientWidth`。
- 检查每个主要容器的子元素是否越过自身边界，不能只检查页面整体滚动宽度。
- 特别检查侧栏表单、双列字段、终端命令输入、文件路径和长主机名。
- 除横向滚动外，还检查主要面板实际宽度，避免内容被压缩成窄列却没有触发页面溢出。
- 检查文字、按钮、输入框和状态卡片是否重叠或被裁切。
- 检查 Grid/Flex 子项是否设置 `min-width: 0`，避免输入框把父容器撑破。
- 文件树或任务详情增长时，低高度桌面的主面板高度不得随内容无限增长；分别检查外层固定高度和内部 `scrollHeight > clientHeight`。

## 数据压力

- 长用户名、长主机名、长 GPU 名称。
- 多个 GPU 卡片和多个并行终端。
- 长文件路径和长命令输出。
- 文件上传、下载、重命名、删除按钮的禁用态、选择态和移动端换行。
- 无 GPU、无磁盘数据、空目录等空状态。

## 最低自动化要求

使用 Playwright 至少生成连接前和模拟连接后的桌面、移动截图，并输出：

- 每个视口的页面宽度与客户端宽度。
- 每个主要容器的越界元素列表。
- 关键区域是否可见、是否被错误隐藏。

截图检查通过后，删除临时截图和模拟脚本，不把测试凭据写入项目。
