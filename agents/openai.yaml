interface:
  display_name: "Remote Control Bridge"
  short_description: "Loopback SSH/SFTP proxy with browser and Agent APIs"
  default_prompt: "Use $remote-control-bridge as the local SSH/SFTP proxy for my authorized remote host."
