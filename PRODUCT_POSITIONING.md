# Skill 定位修订

这个 skill 的主定位不是“深度学习控制台”，而是：

> 跨 Windows、Linux、macOS 的稳定远程 SSH 交互与文件工作台。

## 核心目标

- SSH 会话稳定、可持续、可恢复；
- 网页终端和 Agent API 使用同一套远程会话；
- Bridge API 重启不影响 sessiond 中的 SSH 连接和任务；
- 命令、文件、日志、进程和状态都有友好的交互界面；
- 远程操作默认安全、可审计、可撤销。

## 深度学习只是增强方向

GPU、显存、训练进程、Slurm、tmux 和大文件传输作为可选增强模块，不改变通用远程工作台的基础架构。

## 优先级调整

1. `sessiond` 稳定性、断线恢复和真实 PTY；
2. SFTP 文件浏览、预览、编辑和传输；
3. 进程、日志、CPU、内存、磁盘和网络监控；
4. Agent capability、审批和审计；
5. GPU、训练任务、Slurm 等领域增强。

## 非目标

- 不做通用网络代理；
- 不保存 SSH 密码；
- 不默认修改远程系统配置；
- 不把所有功能绑定到 NVIDIA、conda 或 Slurm。
