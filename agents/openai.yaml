interface:
  display_name: "Remote Control Bridge"
  short_description: "Secure loopback SSH workspace and Agent bridge"
  default_prompt: "Use $remote-control-bridge to inspect and operate my authorized remote SSH host through the local bridge."
